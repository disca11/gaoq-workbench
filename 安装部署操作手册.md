# 高全的工作台 — 安装与部署操作手册

> 适用文件：`server.js`、`public/`、`install-desktop.bat`、`install-desktop.ps1`、`start-server.bat`、`render.yaml`
> 三大动作：① 部署到云端（Render）② 桌面安装（Windows）③ 手机鸿蒙安装

---

## 一、部署到云端（Render）— 让手机/电脑全球实时同步

**前提**：有 GitHub 账号 + Render 账号（render.com，免费注册即可）

**步骤**
1. 把「高全的工作台」整个目录推送到一个 GitHub 仓库（首次：本地 `git init` → 在 GitHub 新建空仓库 → `git add . && git commit -m init && git push`）。
2. 登录 Render → 点 **New → Blueprint** → 授权并选择该 GitHub 仓库。
3. Render 自动读取 `render.yaml`，看到如下配置（无需手改）：
   - `plan: starter` —— 付费最小常驻实例（不休眠，可挂持久磁盘）
   - `disk: workbench-data`，挂载到 `/data`，`DATA_DIR=/data`
   - `startCommand: node server.js`，`healthCheckPath: /`
4. 点 **Create**，等 1–2 分钟部署完成。
5. 拿到形如 `https://gaoq-workbench.onrender.com` 的公网地址 —— 这就是工作台入口。**手机、电脑、平板填这个地址都能用，数据实时同步（WebSocket）。**

**关键点 / 避坑**
- **必须用 starter（不能用 free）**：free 实例会休眠，且仅临时文件系统、不支持挂持久磁盘，redeploy 后数据会丢。starter 已挂 1GB 持久磁盘，数据写在 `/data`，重启/redeploy 都不丢。
- 端口由 Render 自动注入 `PORT` 环境变量，无需手动设。
- 本地开发时机器上没有 `/data`，`server.js` 自动回落到项目目录，行为不变。

---

## 二、桌面安装（Windows 电脑）

安装器会：自动找 Node（优先 PATH，否则用 WorkBuddy 自带管理版）→ 建「高全的工作台」桌面快捷方式 → 设开机自启 → 自动打开独立窗口（Edge/Chrome 的 `--app` 模式，无地址栏，像原生软件）。

### 方式 0：双击即装（最简单，推荐）★
不用命令行、不用传参——地址放在同目录的 `workbench-url.txt` 里：
1. 用记事本打开 `workbench-url.txt`，把你的 Render 公网地址写在第一行（删掉行首的 `#`），保存。
   ```
   https://你的地址.onrender.com
   ```
2. **直接双击** `install-desktop.bat` —— 安装器自动读取这个地址，装好云端版（桌标+开机自启+打开），全程零参数。
- 地址优先级：命令行参数 > `workbench-url.txt` 首行 > 默认本机 `localhost:8787`。
- 若 `workbench-url.txt` 首行留空或仍以 `#` 开头，双击则装本机离线版。

### 场景 A：命令行指定地址（备选）
在文件管理器地址栏或命令行里运行（把地址换成你的）：
```
install-desktop.bat https://你的地址.onrender.com
```
- 数据在云端，本机**不需要常开服务**，双击图标即用。
- 安装器检测到公网地址，跳过本地服务启动，直接开 App 窗口指向云端。

### 场景 B：纯本机离线使用（不部署云端）
直接**双击** `install-desktop.bat`（不带参数、且 `workbench-url.txt` 首行是注释/空），默认 `http://localhost:8787`。
- 安装器会后台启动本机同步服务（`start-server.bat`），再开独立窗口。
- ⚠️ 本机服务需一直运行才能用；换电脑数据不跟随（除非手动导出导入）。

### 说明
- 没装 Edge/Chrome 时退回系统默认浏览器（独立窗口模式可能不可用）。
- 桌面图标：`桌面\高全的工作台.lnk`；开机自启：`启动文件夹\start-workbench.vbs`。
- 重装/换机器：把整个「高全的工作台」目录拷过去，改好 `workbench-url.txt`，再双击 `install-desktop.bat` 即可。

---

## 三、手机鸿蒙安装（HarmonyOS）

鸿蒙没有传统安装包，用 **PWA「添加到主屏幕」** 实现类 App 体验（全屏、无浏览器工具栏、桌面有图标）。

### 方式 1：扫码直达（最简单）
1. 电脑工作台右上角点 **「安装」** 按钮，弹出安装指引弹窗。
2. 弹窗顶部有「手机扫码直达」二维码，地址 = 工作台当前地址（云端部署即 `https://xxxx.onrender.com`）。
3. 鸿蒙手机用相机/扫一扫扫这个码 → 直接打开工作台网页。

### 方式 2：浏览器添加主屏幕（变 App）
1. 手机浏览器打开公网地址 `https://xxxx.onrender.com`。
2. 浏览器右上角菜单 → **「添加到主屏幕」/「安装到桌面」**。
3. 桌面出现「高全的工作台」图标，点开即全屏独立窗口，和 App 一样用。
4. 手机填数据 → 电脑**实时同步**（WebSocket 推送）。

### 说明 / 避坑
- 必须走**公网地址**（Render 的 https 地址）才能「任意网络」访问。若用局域网地址（`192.168.x.x`），手机必须和电脑连**同一 Wi-Fi** 才能扫开。
- 鸿蒙上推荐用手机自带浏览器或 Edge 完成「添加到主屏幕」，体验最接近原生。
- 添加后首次打开若提示「离线不可用」，正常——Service Worker 需联网注册一次缓存，之后离线也能看。

---

## 四、数据备份与安全提醒

- 云端数据存在 Render 持久磁盘，但建议**定期在网页里点「导出」**下载 JSON 备份，防极端情况。
- 浏览器本地也有缓存（离线可改，网络恢复后自动补传）。
- 公网地址任何人拿到都能打开（数据在你的服务端，不在对方机器上），**不要外泄地址**；页面不预填真实隐私数据。

---

## 五、常见问题速查

| 现象 | 原因 / 处理 |
|------|------------|
| 双击 `install-desktop.bat` 报 `Node.js not found` | 没装 Node 且 WorkBuddy 管理版路径异常。确认 WorkBuddy 已安装，或去 nodejs.org 装 Node 后重跑。 |
| 云端打开是空白/老页面 | Render 部署后需等 1–2 分钟；硬刷新（Ctrl+F5）清缓存。 |
| 手机扫码打不开 | 地址是局域网 IP 时手机需同 Wi-Fi；或扫码未跳转，手动复制地址到浏览器。 |
| 数据没了 | 之前用 free 实例 redeploy 会清临时盘；已改 starter+持久磁盘根治。老数据需从 JSON 备份恢复。 |
| 换电脑后桌面图标没了 | 把目录拷过去重跑 `install-desktop.bat`；数据在云端无需迁移。 |
